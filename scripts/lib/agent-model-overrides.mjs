export const BUNDLED_AGENT_MODEL_OVERRIDES = Object.freeze({
  "context-builder": Object.freeze({ thinking: "medium" }),
  delegate: Object.freeze({}),
  oracle: Object.freeze({ thinking: "max" }),
  planner: Object.freeze({ thinking: "xhigh" }),
  researcher: Object.freeze({ thinking: "medium" }),
  reviewer: Object.freeze({ thinking: "high" }),
  scout: Object.freeze({ thinking: "low" }),
  worker: Object.freeze({ thinking: "medium" }),
});
